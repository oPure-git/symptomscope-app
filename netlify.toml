[build]
  publish = "public"
  functions = "netlify/functions"

# Rewrite /api/analyze → the serverless function so the frontend URL never changes
[[redirects]]
  from = "/api/analyze"
  to = "/.netlify/functions/analyze"
  status = 200
